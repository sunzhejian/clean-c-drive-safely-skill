interface:
  display_name: "C Drive Safe Cleanup"
  short_description: "General Windows drive cleanup with safety checks"
  default_prompt: "Use $clean-c-drive-safely to audit my Windows system drive, classify safe cleanup candidates, migrate app caches, and validate affected software."
